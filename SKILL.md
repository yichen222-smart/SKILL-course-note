---
name: course-note
description: >
  通用课堂笔记/复习提纲生成。根据课件（PDF/PPTX）自动生成 Obsidian 格式的结构化课堂笔记。
  当用户提到以下内容时触发：做笔记、整理笔记、课堂笔记、复习提纲、课件整理、根据课件生成笔记、做复习提纲、整理课件、做提纲。
  或者用户提供课件文件路径并要求生成笔记/提纲时触发。
  注意：不要与 financial-english-outline 混淆（后者是专门针对《金融英语阅读教程》扫描版PDF的）。
  此 skill 适用于任何课程的课件，支持 PDF 和 PPTX 格式。
---

# 通用课堂笔记生成 Skill

## 功能概述

读取任意课程的课件文件（PDF 或 PPTX），根据课件内容自动生成 Obsidian 格式的结构化课堂笔记/复习提纲。笔记风格参考财政学笔记格式，强调层级清晰、重点突出、格式规范。

## 核心工作流程

### 第零步：确认课件文件和存储位置

在开始任何操作之前，必须先向用户确认：

1. **识别文件**：
   - 根据用户上下文判断课件文件路径
   - 如果用户已提供路径，直接展示确认
   - 如果用户未提供，主动询问："请提供课件文件的路径（PDF 或 PPTX）"

2. **确认课件**：向用户展示识别的文件路径，询问：
   > "是要根据 [文件路径] 制作笔记吗？"

3. **确认存储位置**：询问：
   > "笔记存储到哪个路径？默认：`课程名/笔记/第X章 标题.md`"
   - 如果用户确认默认路径，按规则生成
   - 如果用户指定其他路径，使用用户提供的路径
   - 支持相对路径（从 vault 根目录算起）

4. **等待确认**：用户确认两个问题后再继续。如果用户说不对，让用户提供正确的文件路径和存储位置。

### 第一步：读取课件内容

根据文件扩展名选择对应的提取方式：

#### PDF 文件（.pdf）
使用 PyMuPDF（fitz）提取文字：

```bash
python -c "
import fitz, json, sys, os
doc = fitz.open(os.path.normpath(sys.argv[1]))
pages = []
for i, page in enumerate(doc):
    text = page.get_text()
    if text.strip():
        pages.append({'page': i+1, 'text': text})
with open('_temp_courseware.json', 'w', encoding='utf-8') as f:
    json.dump(pages, f, ensure_ascii=False, indent=2)
print(f'Extracted {len(pages)} pages with content')
doc.close()
" "D:/Obsidian Vault/课件文件路径.pdf"
```

#### PPTX 文件（.pptx）
使用 python-pptx 提取文字：

```bash
python -c "
from pptx import Presentation
import json, sys, os
prs = Presentation(os.path.normpath(sys.argv[1]))
slides = []
for i, slide in enumerate(prs.slides):
    texts = []
    for shape in slide.shapes:
        if shape.has_text_frame:
            for para in shape.text_frame.paragraphs:
                text = para.text.strip()
                if text:
                    texts.append(text)
    if texts:
        slides.append({'slide': i+1, 'text': '\n'.join(texts)})
with open('_temp_courseware.json', 'w', encoding='utf-8') as f:
    json.dump(slides, f, ensure_ascii=False, indent=2)
print(f'Extracted {len(slides)} slides with content')
prs.close()
" "D:/Obsidian Vault/课件文件路径.pptx"
```

提取后用 Read 工具分块读取 `_temp_courseware.json`，读取完成后删除临时文件：
```bash
rm "_temp_courseware.json"
```

> **如果提取文字较少（如扫描版PDF）**：告知用户文字提取不完整，可能需要 OCR 工具或手动补充。当前 skill 暂不支持 OCR。

### 第二步：确定章节和标题

从课件内容中分析：
- **章节号**：从课件文件名或第一页标题提取（如第几章）
- **章节标题**：从课件主标题提取
- **大节/小节**：从课件中的标题层级识别

文件名规则：`第X章 标题.md`（X 为章节编号）
默认存储路径：`课程名/笔记/第X章 标题.md`
如果用户指定了其他路径，使用用户提供的路径。

### 第三步：生成结构化笔记

#### 结构层级（严格编号）

正文**不从 `# 第X章 标题` 开头**（文件命名已包含章节信息），直接从 `# 1.` 开始：

```
# 1. 大节标题          ← 一级标题
## 1.1 小节标题         ← 二级标题
### 1.1.1 子节标题      ← 三级标题
```

#### 内容处理规则

**保留**：
- 所有核心定义、关键公式、重要分类/对比表格、考试相关数据
- 课件中的框架性标题和大纲层级

**删减**：
- 长篇铺垫性文字、重复性说明、口语化过渡语
- 课件中的动画/过渡页（仅保留实质内容）

**压缩**：
- 长案例缩减为 1-3 句核心事实，保留时间/金额/数量等关键数据

**整合**：
- 多页分散的同类内容合并到同一子节下

#### 格式约定

| 元素 | 格式 |
|------|------|
| **关键术语** | 加粗并标注英文原文，如 `**市场失灵（market failure）**` |
| **核心考点** | `==高亮==` 标记（每页笔记不超过 3-5 处） |
| **案例** | `案例：` 开头，内容精简到事实骨架 |
| **思考题** | `思考：` 引出启发式问题 |
| **补充说明** | `补充：` 引入课外扩展知识（非考点） |
| **核心结论** | 重要推导结果用 `核心结论：` 引导 |
| **优缺点** | `优点：` / `缺点：` 或 `积极影响：` / `负面影响：` 分行列举 |
| **表格** | 课件中的对比表格保留并用 Markdown 表格重绘 |
| **公式** | 数学/经济学公式用 `公式：` 引导，**全部使用 LaTeX 语法**（行内公式用 `$...$`，独立公式用 `$$...$$`） |

#### 语言风格

- 使用中文撰写，关键术语首次出现时标注英文
- 句式简洁，多用冒号列举
- 段落短小，一段不超过 3-4 行
- 重要程度通过缩进层级体现（越靠外层越重要）

### 第四步：输出笔记文件

将生成的笔记保存到用户确认的路径。

#### 输出示例

```markdown
# 1. 公共选择理论概述

## 1.1 公共选择的定义

**公共选择（public choice）**：运用经济学的分析方法来研究政治决策过程的理论。

核心结论：公共选择理论认为政治市场上的参与者（选民、政治家、官僚）也是理性经济人，追求自身利益最大化。

## 1.2 公共选择与私人选择的区别

| 维度 | 私人选择 | 公共选择 |
|------|---------|---------|
| 决策主体 | 个人/企业 | 集体 |
| 决策机制 | 市场价格 | 投票 |
| 利益归宿 | 直接归决策者 | 可能与他人分享 |

# 2. 投票机制

## 2.1 投票悖论

**投票悖论（voting paradox）**：又称孔多塞悖论，指在多数票规则下，投票结果可能出现循环多数，无法达成稳定均衡。

案例：三个投票者对三个方案进行投票，可能出现 $A \succ B \succ C \succ A$ 的循环。

思考：如何打破投票悖论？——设定议程、限制选择范围等。

==多数票规则下，只有单峰偏好才能保证投票结果的稳定性与唯一性。==

### 公式示例

行内公式用单美元符包裹：需求价格弹性 $E_d = \frac{\Delta Q/Q}{\Delta P/P}$

独立公式用双美元符包裹：

$$
\frac{\partial U}{\partial x} = \lambda p_x
$$
```

## 参考范例

可参考 vault 中已有的财政学笔记格式：
- [[财政学/笔记/第0章 导论 财政与财政学.md]]
- [[财政学/笔记/第1章 效率、公平与政府.md]]
- [[财政学/笔记/第2章 公共产品.md]]
- [[财政学/笔记/第3章 公共选择理论.md]]
- [[财政学/笔记/第4章 财政支出理论.md]]

如果 vault 中有对应课程已有的笔记，也请参考其风格保持一致。

## 注意事项

1. 始终保持确认步骤 — 先问用户课件文件对不对、存哪里，再动手
2. 不同课程的课件风格差异大（有的层级清晰，有的较松散），尽力从中提取合理的层级结构
3. 如果课件内容不足以支撑完整结构（如只有零散要点），如实告知用户并生成简化版本
4. 生成笔记后，在回复中给出文件链接方便用户打开查看
5. 如果课件文字提取结果不理想（如扫描版），告知用户并提供替代方案

## 与类似 skill 的区分

| Skill | 适用场景 |
|-------|---------|
| **course-note（本 skill）** | 任何课程的 PDF/PPTX 课件 → 结构化笔记 |
| **financial-english-outline** | 金融英语阅读教程扫描版 PDF → 章节复习提纲 |
| **knowledge-card** | 从已有资料中提取重点 → 知识卡片/思维导图 |
